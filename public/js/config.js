/*app.config( function($routeProvider, $locationProvider){
	$routeProvider
		.when('/', {
			templateUrl: "/views/index"
		})
		.when('/search', {
			templateUrl: "/views/search/index"
		});
	$locationProvider.html5Mode(true);
});*/